#+ endpoint: https://lod.globalbioticinteractions.org/query
#+ defaults:
#+   - url: https://hosted-datasets.gbif.org/eBird/2022-eBird-dwca-1.0.zip

SELECT DISTINCT
  ?doi
  ?uuid
  ?contentId
  ?contentType
  ?archiveUrl
  ?seenAt
  ?provenanceId
  ?activity
WHERE
{
   BIND(<https://ipt.vertnet.org/archive.do?r=msb_mamm> as ?archiveUrl)
   BIND("application/dwca" as ?contentType)
   BIND(<hash://sha256/5b7fa37bf8b64e7c935c4ff3389e36f8dd162f0705410dd719fd089e1ea253cd> as ?provenanceId)

{
    SELECT ?contentId ?seenAt ?uuid ?doi ?activity
WHERE {
  graph ?g1 {
   ?uuid <http://www.w3.org/ns/prov#hadMember> <https://ipt.vertnet.org/archive.do?r=msb_mamm> .
   OPTIONAL { ?uuid <http://www.w3.org/1999/02/22-rdf-syntax-ns#seeAlso> ?doi . }
   <https://ipt.vertnet.org/archive.do?r=msb_mamm> <http://purl.org/dc/elements/1.1/format> "application/dwca" .
  }
  graph ?activity {
    ?activity <http://www.w3.org/ns/prov#used> <https://ipt.vertnet.org/archive.do?r=msb_mamm> .
    ?activity <http://www.w3.org/ns/prov#generatedAtTime> ?seenAt .
    ?contentId <http://www.w3.org/ns/prov#qualifiedGeneration> ?activity .
  }

}
}
  # exclude blank nodes that represent unavailable or non-content
  FILTER(!CONTAINS(str(?contentId), ".well-known/genid" ) )

} ORDER BY DESC(?seenAt) LIMIT 2

